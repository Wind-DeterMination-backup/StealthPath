# Detail

## 这一层是什么
`docs/` 是玩家可视化辅助素材层。

## 这一层具体干了什么
- 当前只承载 `bek-feedback-group.png`
- 该图片被 `README.md` 直接引用，用于展示反馈群二维码

## 实现方式
- 这不是网站文档目录，而是“Markdown 引用静态资源目录”。
- 文件数量很少，说明仓库文档仍以纯文本为主，没有引入更复杂的图表资产系统。

## 与其他层级的关系
- 直接服务根目录 `README.md`。
- 不参与构建，不进入代码逻辑。
